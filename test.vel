#include:stdlib.vlib

string hello = "uwILOVEVEL u"~
int8 hell = 3330~


float64 resu~

//comment test! i love vel!
%{text

    mov rax, 1          ; sys_write
    mov rdi, 1          ; stdout
    mov rsi, meow       ; message address
    mov rdx, meowll     ; message length
    syscall

    mov rax, 60         ; sys_exit
    xor rdi, rdi        ; exit code 0
    syscall


}%
%{data
    meow db "I love vel", 10
    meowll equ $ - meow
}%













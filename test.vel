#include:stdlib.vlib

int32 hello = 30;



void _start(){
    //67
    /{
        comment multiline
    }/

    %%mov rax, 1
    %{
        push rax
        pop rax
        push rbx
        pop rbx

    }%

    int32 a = 6;
    int32 b = 7;
    a $$ b;
    a += b;

}
